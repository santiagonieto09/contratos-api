# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in this file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/dataAccess/entity/'
            - '../src/domain/models/'
            - '../src/Kernel.php'

    # Registrar controladores específicamente
    App\presentation\controller\:
        resource: '../src/presentation/controller/'
        tags: ['controller.service_arguments']

    # Registrar servicios del dominio
    App\domain\services\:
        resource: '../src/domain/services/'

    # Registrar repositorios
    App\dataAccess\repository\:
        resource: '../src/dataAccess/repository/'

    # Configuración de las estrategias de método de pago (patrón Strategy)
    App\domain\services\metodoPago\EstrategiaPayPal:
        tags: ['app.metodo_pago']

    App\domain\services\metodoPago\EstrategiaPayOnline:
        tags: ['app.metodo_pago']

    # Servicio de cálculo de cuotas con inyección de estrategias
    App\domain\services\CalculadoraCuotasServicio:
        arguments:
            $metodosDisponibles: !tagged_iterator app.metodo_pago
